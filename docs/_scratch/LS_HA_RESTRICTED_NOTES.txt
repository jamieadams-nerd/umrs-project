## Appears as "Unalabled" but we should mark it as "restricted"
A deiscrepency with our tool and the system's /bin/ls

When running our umrs-selinux examples/ls_ha command on the /var/log 

```
  MODE        IOV  OWNER:GROUP    MODIFIED             NAME
====================================================================================================

<unlabeled> :: <no-level>                                                                           
  drwx------  -O-  root:root      2025-10-14 20:00     aide/
  drwx------  -O-  root:root      2026-02-09 21:34  ⊕  audit/
  drwxr-x---  -O-  chrony:chrony  2025-11-19 19:00     chrony/
```

However, when you run the /bin/ls command on /var/log, 
```
drwx------. 2 root   root   system_u:object_r:aide_log_t:s0       6 Oct 14 20:00 aide/
drwxr-xr-x. 2 root   root   system_u:object_r:var_log_t:s0     4096 Dec  8 00:09 anaconda/
drwx------. 2 root   root   system_u:object_r:auditd_log_t:s0    42 Feb  9 21:34 audit/
```

## The "Restricted" Status
Modify the source code to support the following:

- if the umrs-selinux::secure_dirent generates the NoSelinux error, then instead mark it 
  as "restricted" instead of unlabeled. 
  This applies to the no-level the code was marking. 
- if mcstrans is running but umrs-selinux::secure_dirent can only get the raw sensitivity 
  category set, then we can consider it "unlalabeled". Just means the system doesn't have
  a defined human readable string for it's security label.
- In umrs-selinux::utils::dirlist change the grouping key to accept unlabeled and restricted.
- Remember that examples/ls_ha --> umrs-selinux::utils::dirlist --> umrs-selinux::secure_dirent. 
- Ask user for clarifications. 


## ChatGPT analysis says
- "High-Assurance" question an auditor will ask. 
- The discrepancy between ls -Z and your ls_ha tool isn't a bug; 
   - it is a Verification Gap that exists in standard Linux tools.

Here is exactly why firewalld (or shadow) is "Restricted" in our tool but visible in ls -Z:

1. String-Based Guessing (ls -Z)
- The standard ls command uses a syscall called lstat. 
- The Logic: lstat asks the kernel: "Tell me the label for the string '/etc/firewalld'." 
- The Vulnerability: The kernel looks at the path and returns a label. 
   - However, as the user jadams, you never actually "touched" the file. You just asked about its name. 
   - This is susceptible to TOCTOU (Time-of-Check to Time-of-Use) attacks where a symlink or a
     rename could swap the file between the time you "look" at it and the time you "use" it.

2. Inode-Anchored Verification (ls_ha)

- Our tool follows the NSA Raise the Bar (RTB) principle of Non-Bypassability.
- The Logic: We don't trust the path string. 
  - We attempt to open() the file to get a File Descriptor (FD). -
  - This anchors us to the physical Inode on the disk.
- The Conflict: On RHEL 10, directories like /etc/firewalld or files like /etc/shadow have 
  DAC permissions (like 0700 or 0600) and MAC labels (like firewalld_etc_rw_t) that explicitly 
  prevent the jadams user from opening them.
- The Result: Because open() fails, we cannot call fgetxattr(fd, ...) to read the Ground Truth 
  label directly from the hardware.


## Source code modifications : The "Restricted" Status

- if the umrs-selinux::secure_dirent generates the NoSelinux error, then instead mark it 
  as "restricted" instead of unlabeled. 
  This applies to the no-level the code was marking. 
- if mcstrans is running but umrs-selinux::secure_dirent can only get the raw sensitivity 
  category set, then we can consider it "unlalabeled". Just means the system doesn't have
  a defined human readable string for it's security label.
- In umrs-selinux::utils::dirlist change the grouping key to accept unlabeled and restricted.
- Remember that examples/ls_ha --> umrs-selinux::utils::dirlist --> umrs-selinux::secure_dirent. 
- Ask user for clarifications. 


## Why ls_ha is "restricted"?

When our tool says <RESTRICTED>, it is telling the auditor:
- "I can see the name of this object in the directory list, but the security policy prevents me 
  from anchoring to its Inode. Therefore, I cannot verify its security attributes with high-assurance."

Standard ls -Z is essentially showing you a "Marketing Label"—it's what the system claims is there. Your ls_ha tool is showing you "Verified Metadata"—only what it can actually touch and prove.
How to explain this to the "People":

If someone asks why ls -Z shows the label but UMRS says <RESTRICTED>, you give them the Principal's Answer:
- "Standard ls asks the kernel for a gossip-report based on a filename. 
- UMRS attempt to establish a secure 'Chain of Custody' by opening the file handle. 
  - If the kernel denies us an open handle, we cannot verify the label at the Inode level. 
  - We mark it Restricted to signal that a gap exists between the directory visibility and the security metadata access."


## Background Information and Analysis 
- Have the security-auditor, security-engineer, and tech-writer to understand read the following
  information so they can understand and remember the following. These agents should ask me
  questoins during the planning phase. 
- Do not clutter the source too much but source code comment should be near the assignment
  of restricted assigment or a refreence to other documentation to explain this.

ChatGPT analysis says                                                                            
- "High-Assurance" question an auditor will ask.                                                    
- The discrepancy between ls -Z and your ls_ha tool isn't a bug;                                    
   - it is a Verification Gap that exists in standard Linux tools.                                  
                                                                                                    
Here is exactly why firewalld (or shadow) is "Restricted" in our tool but visible in ls -Z:         
                                                                                                    
1. String-Based Guessing (ls -Z)                                                                    
- The standard ls command uses a syscall called lstat.                                              
- The Logic: lstat asks the kernel: "Tell me the label for the string '/etc/firewalld'."            
- The Vulnerability: The kernel looks at the path and returns a label.                              
   - However, as the user jadams, you never actually "touched" the file. You just asked about its name. 
   - This is susceptible to TOCTOU (Time-of-Check to Time-of-Use) attacks where a symlink or a      
     rename could swap the file between the time you "look" at it and the time you "use" it.        
                                                                                                    
2. Inode-Anchored Verification (ls_ha)                                                              
                                                                                                    
- Our tool follows the NSA Raise the Bar (RTB) principle of Non-Bypassability.                      
- The Logic: We don't trust the path string.                                                        
  - We attempt to open() the file to get a File Descriptor (FD). -                                  
  - This anchors us to the physical Inode on the disk.                                              
- The Conflict: On RHEL 10, directories like /etc/firewalld or files like /etc/shadow have          
  DAC permissions (like 0700 or 0600) and MAC labels (like firewalld_etc_rw_t) that explicitly      
  prevent the jadams user from opening them.                                                        
- The Result: Because open() fails, we cannot call fgetxattr(fd, ...) to read the Ground Truth      
  label directly from the hardware.                     

## Documentation
- The examples/ls_ha will eventually become the umrs-ls tool. 
- The tech-writer has NO assignments during this phase. 
- This work is primarily for security-auditor very quick review during the planning phase.


