# umrs-translator Agent Memory

## i18n Crate
- Confirmed: `gettextrs` (gettext-rs) — in umrs-ls Cargo.toml as of 2026-03-10
- Extraction tool: `xtr` (Rust-aware companion to gettextrs)
- xtr install: `cargo install xtr`
- xtr was NOT installed on the build host as of 2026-03-10 — check before extraction runs

## Domain Assignments

| Crate | Type | Domain | Directory |
|---|---|---|---|
| `umrs-ls` | binary | `umrs-ls` | `resources/i18n/umrs-ls/` |
| `umrs-state` | binary | `umrs-state` | `resources/i18n/umrs-state/` |
| `umrs-logspace` | binary | `umrs-logspace` | `resources/i18n/umrs-logspace/` |
| `umrs-core` | library | `umrs-core` | `resources/i18n/umrs-core/` |
| `umrs-platform` | library | `umrs-platform` | `resources/i18n/umrs-platform/` |

Full registry: `resources/i18n/domains.md`

## Pipeline Files (umrs-logspace)
- `resources/i18n/umrs-logspace/umrs-logspace.pot` — re-extracted 2026-03-10; msgids are bare labels (no trailing colon or spaces)
- `resources/i18n/umrs-logspace/fr_CA.po` — updated 2026-03-10; msgids corrected to match new pot; translated, validated, compiled
- `resources/i18n/umrs-logspace/fr_CA.mo` — compiled; regenerated by `make i18n-compile-umrs-logspace`
- NOTE: Old fr_CA.po had trailing `: ` in msgids (e.g. "Resource Pool:"); source was refactored to move colon into format string

## Pipeline Files (umrs-state)
- `resources/i18n/umrs-state/umrs-state.pot` — 8 msgids; generated 2026-03-10 by xtr (line numbers slightly stale vs current source — re-run xtr when available)
- `resources/i18n/umrs-state/fr_CA.po` — translated 2026-03-11; all 8 msgids translated, validated, compiled
- `resources/i18n/umrs-state/fr_CA.mo` — compiled 2026-03-11

## Pipeline Files (umrs-ls)
- `resources/i18n/umrs-ls/umrs-ls.pot` — hand-crafted on 2026-03-10 (one string; re-run xtr when installed)
- `resources/i18n/umrs-ls/fr_CA.po` — translated, validated, compiled
- `resources/i18n/umrs-ls/fr_CA.mo` — compiled binary; regenerated by `make i18n-compile-umrs-ls`

## xtr Invocation Pattern (umrs-ls)
```
xtr --package-name umrs-ls \
    --output resources/i18n/umrs-ls/umrs-ls.pot \
    components/rusty-gadgets/umrs-ls/src/main.rs
```
Run from repo root. Re-run after any new `gettext()` wrap in the source.

## Makefile Targets (repo root Makefile)
- `make i18n` — full pipeline: extract + merge + compile (umrs-ls)
- `make i18n-check` — msgfmt --check on all active .po files
- `make i18n-extract-umrs-ls` — xtr extraction only
- `make i18n-merge-umrs-ls` — msgmerge existing .po files against updated .pot
- `make i18n-compile-umrs-ls` — msgfmt .po → .mo
- `make i18n-setup` — scaffold directory structure (generic, no translation)
- `make i18n-build` — generic .po → .mo loop (pre-existing; covers umrs-logspace, umrs-ps, umrs-df)

## Vocabulary Decisions
- "access denied" → "accès refusé" — UMRS decision; no GNU corpus match; standard Unix/Linux francophone
- "MODE" (column header) → "DROITS" — corpus:coreutils informed; coreutils uses "droits" for mode/permissions throughout
- "MARKING" (SELinux marking column) → "MARQUAGE" — UMRS decision; OTAN/NATO and ANSSI francophone term for classification marking
- "OWNER:GROUP" → "PROPRIO:GROUPE" — corpus:coreutils (chown.c "[OWNER][:[GROUP]]" → "[PROPRIO][:[GROUPE]]")
- "SIZE" → "TAILLE" — corpus:tar (genfile.c "SIZE" → "TAILLE"); also coreutils "Size" → "Taille"
- "MODIFIED" (mtime column) → "MODIFIÉ" — UMRS decision; no corpus match; ANSSI/POSIX standard
- "NAME" → "NOM" — corpus:coreutils (who.c) and corpus:tar (tar.c/genfile.c)
- "<restricted>" → "<restreint>" — corpus:bash (builtins/common.c "restricted" → "restreint"); angle-bracket format preserved
- "Resource Pool" → "Groupe de ressources" — UMRS decision; no corpus match; standard francophone infrastructure terminology
- "Mount point" → "Point de montage" — corpus:coreutils (find-mount-point.c %m format string and ~msgid entries)
- "Total space" → "Espace total" — UMRS decision; no corpus match; standard francophone POSIX/Linux usage
- "Free space" → "Espace libre" — UMRS decision; no corpus match; standard francophone POSIX/Linux usage
- "Lifecycle" → "Cycle de vie" — UMRS decision; no corpus match; ANSSI/OTAN terminology for data retention lifecycle
- "Starting [tool]" → "Démarrage de [tool]" — UMRS decision; standard francophone Linux startup-message pattern
- "FIPS is Enabled" → "FIPS est activé" — "activé" corpus-attested (coreutils/bash); FIPS acronym retained
- "System Purpose" → "Objet du système" — UMRS decision; RHEL francophone subscription tooling term
- "state file" → "fichier d'état" — UMRS decision; standard francophone Linux/POSIX term
- "Warning" (prefix) → "Avertissement" — corpus:coreutils/bash; well attested
- "(not set)" → "(non défini)" — UMRS decision; standard francophone Linux/POSIX placeholder
- "Unknown key" → "Clé inconnue" — corpus:findutils ("inconnu(e)"); feminine agreement with "clé"
- "Invalid boolean value — using false" → "Valeur booléenne invalide — utilisation de faux" — UMRS decision
- See `resources/i18n/vocabulary-fr_CA.md` for full term table

## French Typography Rules
See [french_typography.md](french_typography.md) — Jamie's canonical rules (from `.claude/jamies_brain/french_quotes.txt`).
- Use « » with interior spaces; never English " "
- Guillemets for: quotations, literal program output, first-use technical terms, word-as-word
- No guillemets for: acronyms, proper names, code, file paths, commands
- Commands/code/paths: monospace backtick format only
- Nested quotes: « outer "inner" »

## Key File Paths
- Vocabulary: `resources/i18n/vocabulary-fr_CA.md`
- Domain registry: `resources/i18n/domains.md`
- Makefile: `/media/psf/repos/umrs-project/Makefile`
- Corpus skill: invoke `french-lookup` skill — do NOT grep corpus files directly
- IMPORTANT: Corpus files are in `.claude/corpus/*.po` (NOT `.claude/corpus/gnu-fr/`). The lookup script has a bug in its path; it works because the fallback grep covers `$CORPUS/*.po`. The skill must be invoked via `bash /media/psf/repos/umrs-project/.claude/commands/french_lookup.sh "<term>"` from the repo root when the SKILL.md path is wrong.
- Corpus files present: coreutils, util-linux (missing), grep, sed, tar, findutils, bash, cryptsetup

## Cargo.toml Entry (confirmed)
- `gettext-rs = { version = "0.7", features = ["gettext-system"] }` — workspace-wide entry
- Use `gettext()` function; `gettext!()` macro does NOT exist in gettext-rs 0.7
- For interpolated strings: `format!("{}", gettext("..."))` or `format!("{} ({n}) ", gettext("..."))`
- Primary locale for all UMRS crates: `fr_CA`

## Canonical "translate for [crate]" Workflow

This is the confirmed, user-approved workflow. Execute these steps in order when asked to
"translate for [crate]" or to onboard a new crate to the i18n pipeline.

1. **Check prerequisites**
   - Run `which xtr` or `xtr --version` to confirm it is installed.
   - If missing: report `cargo install xtr` as the install command.
   - Do not run the extraction step, but continue with all other steps.
   - A hand-crafted `.pot` is acceptable as a placeholder when `xtr` is unavailable,
     provided the wrapped strings are already known from source inspection.
   - The hand-crafted `.pot` must be regenerated with `xtr` once it is installed.

2. **Confirm domain assignment**
   - Read `resources/i18n/domains.md`; create it if it does not exist.
   - Binary crates get one domain named after the crate.
   - Library crates each get their own domain (for reuse across binaries).
   - Add the new crate row to the domain table before any extraction work begins.

3. **Run french-lookup skill**
   - Invoke the `french-lookup` Skill tool for each primary technical term.
   - Do this before proposing any translation — corpus result is authoritative.
   - If no corpus match: consult `resources/i18n/vocabulary-fr_CA.md`.
   - If neither source covers the term: retain English and add a `# TRANSLATOR:` comment.

4. **Extract strings**
   - Run `xtr` to produce `resources/i18n/<domain>/<domain>.pot`:
     ```
     xtr --package-name <domain> \
         --output resources/i18n/<domain>/<domain>.pot \
         <source files>
     ```
   - Run from repo root. Re-run after any new `gettext()` wrap in source.
   - If `xtr` unavailable: hand-craft the `.pot` from confirmed source locations.

5. **Initialize locale**
   - If `resources/i18n/<domain>/fr_CA.po` does not already exist:
     ```
     msginit --input=resources/i18n/<domain>/<domain>.pot \
             --locale=fr_CA \
             --output=resources/i18n/<domain>/fr_CA.po \
             --no-translator
     ```

6. **Translate**
   - Replace the `msginit`-generated header with the UMRS standard `.po` header
     (product name = crate name; see header template in system prompt).
   - Fill in `msgstr` values using corpus terms or vocabulary-fr_CA.md.
   - Add `# TRANSLATOR:` comments for any term requiring explanation or judgment.

7. **Validate and compile**
   - `msgfmt --check resources/i18n/<domain>/fr_CA.po`
   - `msgfmt -o resources/i18n/<domain>/fr_CA.mo resources/i18n/<domain>/fr_CA.po`
   - Both must succeed before proceeding.

8. **Update vocabulary file**
   - Read `resources/i18n/vocabulary-fr_CA.md` before the session; update it after.
   - Add any new term decisions with Source and Notes columns.
   - Also update `.claude/agent-memory/umrs-translator/vocabulary.md`.

9. **Update top-level Makefile**
   - Read `/media/psf/repos/umrs-project/Makefile` first (required before any edit).
   - Add `<domain>` to `I18N_TEXT_DOMAINS`.
   - Add `I18N_SOURCES_<domain>` variable mapping domain to source files.
   - Add `I18N_ACTIVE_LOCALES_<domain>` variable listing translated locales.
   - Add per-domain targets: `i18n-extract-<domain>`, `i18n-merge-<domain>`,
     `i18n-compile-<domain>`.
   - Extend `i18n` target to depend on the three new per-domain targets.
   - Extend `i18n-check` to validate the new domain's `.po` files.
   - Add new phony targets to the top-level `.PHONY` declaration.
   - Verify with `make i18n-check` after editing.

## umrs-core i18n Pattern (canonical reference)

File: `components/rusty-gadgets/umrs-core/src/i18n.rs`

- Two public functions: `init(domain: &'static str)` and `tr(msgid: &str) -> String`
- Library-scoped function: `tr_core(msgid)` — uses `dgettext("umrs-core", msgid)`
- `OnceLock<()>` for locale init guard; `OnceLock<&'static str>` for domain
- `DEFAULT_LOCALEDIR = "/usr/share/locale"`; `FALLBACK_DOMAIN = "umrs"`
- No `build.rs` in `umrs-core` — `.mo` compilation is a manual pipeline step

For `umrs-platform`, the library-scoped function should be named `tr_platform()`
with `dgettext("umrs-platform", msgid)`.

## xtr Invocation Pattern (umrs-platform)

```bash
xtr --package-name umrs-platform \
    --output resources/i18n/umrs-platform/umrs-platform.pot \
    components/rusty-gadgets/umrs-platform/src/**/*.rs
```

Run from repo root. Adjust glob expansion per shell.

## thiserror Wrapping Decision

`#[error("...")]` attributes on `thiserror`-derived enums cannot be wrapped
with `tr_platform()` at the annotation site. Agreed approach: leave `#[error]`
strings in English; wrap at the binary display boundary where errors are surfaced
to users. Record these strings in the `.pot` for translation inventory purposes.

## Scope Decision Pending

Before library log string wrapping is implemented, confirm with Jamie: should
`warn!`/`error!` strings in library crates be wrapped at runtime (adds `gettextrs`
lookup per call) or only at the binary display layer?
See `resources/i18n/reports/2026-03-11-umrs-platform-unwrapped.md`.

## umrs-tui Domain (added 2026-03-20)

- Domain: `umrs-tui` (binary) — confirmed at `main.rs:1906`: `i18n::init("umrs-tui")`
- Audit report: `.claude/reports/umrs-tui-i18n-audit-2026-03-20.md`
- Status: strings identified; no wrapping done yet; xtr not run yet
- ~55 strings already wrapped; ~45 unwrapped in runtime fns; ~50+ in const fns
- Key constraint: `indicator_description()`, `indicator_recommended()`,
  `help_text_for_tab()` are `const fn` — must be converted to regular `fn`
  before wrapping. Developer action required before extraction can proceed.
- KEY_LEGEND in `status_bar.rs` is `const` — low priority, defer.
- Do NOT translate: distro names (RHEL, Fedora, etc.), os-release field names,
  POSIX API token strings (PROC_MAGIC etc.), log messages.
- xtr invocation for umrs-tui when ready:
  `xtr --package-name umrs-tui --output resources/i18n/umrs-tui/umrs-tui.pot`
  + all .rs files in umrs-tui/src/ (see report for full file list)
