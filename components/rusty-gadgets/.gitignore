# components/rusty-gadgets/.gitignore

# Cargo build output
/target/

# Staging directory — ephemeral output of `cargo xtask stage`.
# Never commit. Regenerated by `make build` or `make release`.
/staging/
